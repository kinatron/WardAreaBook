<% if update %>
  <% remote_form_for @family do |f| %>
    <%= f.error_messages %>
    <p>
      <% if @family.member == false %>
        <%= f.select :status, nonmemberType %>
      <% else %> 
        <%= f.select :status, memberStatus %>
      <% end %>
      &nbsp;
      <%= f.submit 'Update' %>
    </p>
  <% end %>
<% else %>
  <span style='background-color: <%= statusColor(@family.status) -%>'>
    <%= link_to_remote @family.status, :url => {:action => 'edit_status', 
      :id => @family} %>&nbsp;
  </span>
<% end %>
